%h4
  Team allocation
.row
  .col-md-3
    = render(partial: "available_employees", locals: { available_employees: company.available_employees })
  .col-md-9
    = render(partial: "team", collection: company.teams)
.clearfix

