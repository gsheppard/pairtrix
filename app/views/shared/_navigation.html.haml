%nav.navbar.navbar-default.navbar-fixed-top(role="navigation")
  .navbar-header
    %button.navbar-toggle(data-target=".navbar-collapse" data-toggle="collapse")
      %span.sr-only
        Toggle navigation
      %span.icon-bar
      %span.icon-bar
      %span.icon-bar
    = link_to("Pairtrix", root_url, class: "navbar-brand")
  .collapse.navbar-collapse
    %ul.nav.navbar-nav
      %li
        = link_to("Help", help_url)
      = available_teams_navigation_dropdown
    = render("shared/sign_in_sign_out")
